set:
  - (set|start|create) a? timer (for|of) .duration.
  - timer (for|of) .duration.

cancel:
  - (cancel|stop|end) the? timer

query:
  - how (long|much time) is? (left|remaining) (on the timer)?
  - when (will|does) the timer (go off|expire|end)
